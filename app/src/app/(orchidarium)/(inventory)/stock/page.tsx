import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Gestión de Stock',
}

export default function StockPage() {
  return (
    <div>
      <h1>(inventory)/stock Page</h1>
    </div>
  )
}
