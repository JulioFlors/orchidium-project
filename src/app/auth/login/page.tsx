import { titleFont } from '@/config/fonts'

export default function LoginPage() {
  return (
    <div className={`${titleFont.className} my-7 text-4xl font-bold italic antialiased`}>
      <h1>Login Page</h1>
    </div>
  )
}
